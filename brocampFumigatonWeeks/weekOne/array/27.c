#include <stdio.h>
int main(void)
{
    int i,j,n,arr[10],k=0;
    printf("Enter array limit: ");
    scanf("%d",&n);
    printf("Enter array values:\n");
    for(i=0;i<n;i++)
    {
        scanf("%d",&arr[i]);
    }
    for(i=0;i<n;i++)
    {
        if(arr[i]==0)
        {
            
            
        }
    }
    for(i=0;i<n;i++)
    {
        printf("%d ",arr[i]);
    }
}